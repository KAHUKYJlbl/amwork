import { HashtagType } from '../../lib/types';

const HashtagList: HashtagType[] = [
  {type: 'highlight', text: 'Entity title'},
  {type: 'pointed', text: 'Front-end'},
  {type: 'regular', text: 'Entity title'},
];

export default HashtagList;